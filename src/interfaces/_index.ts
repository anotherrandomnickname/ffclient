import IStore from '@/interfaces/IStore'
import ServerResponse, { asServerResponse } from '@/interfaces/ServerResponse'
import ServerError, { isServerError } from '@/interfaces/ServerError'
import { MouseEvent, MouseEventValue } from '@/interfaces/MouseEvent'

export {
  IStore,
  MouseEvent,
  MouseEventValue,
  ServerResponse,
  asServerResponse,
  ServerError,
  isServerError
}

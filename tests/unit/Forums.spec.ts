import { expect } from 'chai'
import { createLocalVue } from '@vue/test-utils'
import {} from 'mocha'
import Vuex, { Store } from 'vuex'
/* import Forums from '@/store/modules/Forum' */

/* const localVue = createLocalVue()
localVue.use(Vuex)

describe('Vuex: Forums', () => {
  const store: Store<any> = new Vuex.Store({ modules: { forums: Forums } })

  const forums = store.state.forums

  it('Должен вынимать валидные данные по форумам', async done => {
    const pk = 0

    await ForumHandler.fetchForumPage().then((data: any) => {
      expect(data).not.equal(undefined)
      expect(data.data).to.be.an('array')
      expect(data.data[0].fPk).not.equal(undefined)
      expect(data.data[0].fPk).equal(1)
      done()
    })
  })
})
 */
